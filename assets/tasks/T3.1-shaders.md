# T3.1 — Reference shaders (hand-authored)

> Read [`_SHARED.md`](_SHARED.md) first. Two-agent task: Implementer + separate Verifier.

## Dependencies
T2.1 (sprite highlight mask), T2.2 (tile highlight mask) — mask format must be final.

## Inputs (read-only)
- `experiment/shaders/` — verified day/night decomposition (`daynight_live.glsl`,
  `daynight_bank.glsl`, `daynight_lut.json`, `compare.py`, `FINDINGS.md`)
- `src/fmain2.c` `fade_page()` (effect source of truth)

## Implementation
Hand-author GLSL (with pseudocode comments — not generated) under `assets/shaders/`:
- `fade_to_black.glsl` — uniform multiply (also fade-from-black / transitions).
- `daynight_dim.glsl` — `lightlevel`-driven uniform brightness scale.
- `region_crossfade.glsl` — lerp two RGBA region renders over 8 frames.
- `moonlight_blue.glsl` — `b += g2*g`; night channel floors r≥10%, g≥25%, b≥60%.
- `green_jewel.glsl` — `r = max(r, g)`.
- `daynight_live.glsl` / `daynight_bank.glsl` — full day/night incl. vegetation night boost;
  port the verified reference from `experiment/shaders/`.
- `README.md` — inputs/uniforms per shader; indexed-path vs RGBA+`highlight_mask` matrix.

## Outputs
- `assets/shaders/*.glsl` + `assets/shaders/README.md`

## Implementer self-check
- Day/night shaders reproduce `experiment/shaders/compare.py` reference; README maps each effect.

## Verification (DIFFERENT agent)
1. **Bit-exact day/night:** run/replicate `experiment/shaders/compare.py` logic against the
   ported `daynight_live.glsl` + `highlight_mask` (and the `daynight_bank` path) at **every**
   light level; require zero diff vs the `fade_page()` reference.
2. **Formula audit:** read `fade_page()` in `fmain2.c` and confirm `moonlight_blue`, `green_jewel`,
   and the channel floors match the source arithmetic exactly.
3. Confirm README documents uniforms/inputs for every shader and the indexed-vs-RGBA+mask matrix.
4. Confirm mask semantics in the shaders match the emitted masks (index ∈ 16–24).
Report PASS/FAIL with the light-level diff result.
